# self_cutedsl — the fully open-sourced CuTeDSL-compatible stack.
#
# Frontend (AST rewrite + tracing), IR bridge to the public cute dialect,
# selfcute dialects (kernel/pipeline/sm120), and the CUDA-Driver-JIT runtime.

__version__ = "0.1.0"
